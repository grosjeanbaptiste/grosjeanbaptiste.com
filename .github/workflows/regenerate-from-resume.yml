name: Regenerate index.html from resume.json

on:
  push:
    branches: [master]
    paths:
      - 'assets/data/resume.json'
      - 'assets/data/i18n/**'
      - 'scripts/generate-from-resume.js'
      - '.github/workflows/regenerate-from-resume.yml'
  workflow_dispatch:

jobs:
  regenerate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Regenerate
        run: node scripts/generate-from-resume.js

      - name: Commit changes if any
        run: |
          if [[ -z "$(git status --porcelain index.html fr/ nl/ es/)" ]]; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html fr/ nl/ es/
          git commit -m "chore: regenerate localized index.html from resume.json"
          git push
